

// найти угол поворота между векторами
// повернуть что-то на точку
//        Eigen::Vector3f initialUp(0, 1, 0);
//        Eigen::Vector3f planetNormal = Eigen::Vector3f(x, y, z);
//        planetNormal.normalize();
//        Eigen::Vector3f rotationAxisNormal = initialUp.cross(planetNormal);
//        rotationAxisNormal.normalize();
//        float angleNormal = acos(initialUp.dot(planetNormal));
//        Eigen::Matrix4f rotationMatrixNormal = EigenGL::createRotationMatrixAxis(angleNormal, rotationAxisNormal);
//        Eigen::Matrix4f testRotationMatrix = EigenGL::createRotationMatrixAxis(M_PI / 2, planetNormal);